{
  "name": "pdi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "nest build",
    "migration:generate": "yarn build && typeorm migration:generate -d src/infra/postgres/typeorm/migration -p -n",
    "migration:show": "yarn build && typeorm migration:show",
    "migration:create": "node --require ts-node/register ./node_modules/typeorm/cli.js migration:create -n",
    "migration:revert": "node --require ts-node/register ./node_modules/typeorm/cli.js migration:revert"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/bcryptjs": "^2.4.4",
    "@types/jest": "^29.5.5",
    "@types/node": "^20.6.5",
    "@types/passport-jwt": "^3.0.10",
    "@types/ramda": "^0.29.4",
    "jest": "^29.7.0",
    "ramda": "^0.29.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@nestjs/common": "^10.2.6",
    "@nestjs/core": "^10.2.6",
    "@nestjs/jwt": "^10.1.1",
    "@nestjs/passport": "^10.0.2",
    "@nestjs/typeorm": "^10.0.0",
    "bcryptjs": "^2.4.3",
    "env-var": "^7.4.1",
    "passport-jwt": "^4.0.1",
    "typeorm": "^0.3.17",
    "typeorm-transactional-cls-hooked": "^0.1.21"
  }
}
